name: Supabase RLS Coverage

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  rls-coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate sync table RLS/policy coverage
        run: ./scripts/check_sync_rls_coverage.sh
